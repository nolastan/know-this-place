# Local Claude Code settings (machine-specific permissions, etc.)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
.cache/

# Local mirror of source corpora (large, re-downloadable). See DATA-SOURCES.md
# → loc-newspapers. Facts extracted from these are cited by URL on the pages.
sources/

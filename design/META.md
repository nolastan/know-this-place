# Meta-learning notes

What works about *learning* in this module, as opposed to what works about
design. Written by `/design` in its deferred learning step; nothing else writes
here.

Things worth recording:

- A prompt or question that reliably produced a good correction (e.g. asking for
  the ranking out loud before drawing).
- A layer that keeps receiving the wrong kind of entry, and the test that fixed
  the placement.
- A learning that was promoted too early, or held tentative too long.
- A rule of thumb that turned out to be a bad distillation of its source, and
  why.
- Anything about the loop itself: when the deferred step is too heavy, when
  queueing observations mid-session loses them, when the gap log stops being
  scannable.

Same confidence convention as the other layers: *tentative* until it holds
across sessions.

---

*Empty. Seeded with the module; the module fills it as it goes.*
